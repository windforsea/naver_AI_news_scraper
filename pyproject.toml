[project]
name = "news-scrapwithai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "openai>=3.10.0",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "pywebview>=6.2.1",
    "requests>=2.34.2",
]

[project.scripts]
news-scrapwithai = "news_scrapwithai:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
